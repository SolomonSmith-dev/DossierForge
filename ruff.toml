# Pin the lint rule selection so results are stable across ruff versions.
# These are ruff's classic default rules (pyflakes + a subset of pycodestyle),
# which this project has always been linted against.
[lint]
select = ["E4", "E7", "E9", "F"]
